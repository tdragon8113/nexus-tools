<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.nexus.workspace.mapper.ActivityMapper">

    <resultMap id="ActivityResultMap" type="com.nexus.workspace.entity.Activity">
        <id column="id" property="id"/>
        <result column="user_id" property="userId"/>
        <result column="title" property="title"/>
        <result column="category" property="category"/>
        <result column="start_time" property="startTime"/>
        <result column="end_time" property="endTime"/>
        <result column="duration_minutes" property="durationMinutes"/>
        <result column="notes" property="notes"/>
        <result column="version" property="version"/>
        <result column="created_at" property="createdAt"/>
        <result column="updated_at" property="updatedAt"/>
    </resultMap>

    <select id="findById" resultMap="ActivityResultMap">
        SELECT * FROM activities WHERE id = #{id}
    </select>

    <select id="findByUserId" resultMap="ActivityResultMap">
        SELECT * FROM activities WHERE user_id = #{userId} ORDER BY start_time DESC
    </select>

    <select id="findByUserIdAndDateRange" resultMap="ActivityResultMap">
        SELECT * FROM activities WHERE user_id = #{userId}
        AND start_time BETWEEN #{startTime} AND #{endTime}
        ORDER BY start_time ASC
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO activities (user_id, title, category, start_time, end_time, duration_minutes, notes, created_at)
        VALUES (#{userId}, #{title}, #{category}, #{startTime}, #{endTime}, #{durationMinutes}, #{notes}, #{createdAt})
    </insert>

    <delete id="deleteById">
        DELETE FROM activities WHERE id = #{id}
    </delete>
</mapper>